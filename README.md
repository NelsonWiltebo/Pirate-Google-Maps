Tests for "main.mts" can be found in the "test-cases" branch. Because of the use of a website we had to 
compile "main.ts" with a different module which doesn't work with Jest, the tests are therefore written 
in a separate branch, where "main.ts" can be compiled with a module compatible with Jest.
